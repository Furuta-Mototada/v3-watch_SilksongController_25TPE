# Silksong Motion Controller Dependencies
# Core library for keyboard control
pynput>=1.7.0

# Network service discovery for automatic connection
zeroconf>=0.131.0

# Phase III: Machine Learning Pipeline Dependencies
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
joblib>=1.3.0
jupyter>=1.0.0
notebook>=7.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Phase V: Deep Learning Dependencies
tensorflow>=2.10.0
keras>=2.10.0
h5py>=3.7.0

# Audio Recording for Data Collection (Phase V)
sounddevice>=0.4.6
numpy>=1.24.0  # Already listed above, but needed for audio processing

# Note: Audio recording requires PortAudio library
# Linux: sudo apt-get install portaudio19-dev
# macOS: brew install portaudio
# Windows: Usually included with sounddevice

# Note: Whisper for post-processing (run separately on your machine)
# Install locally: pip install openai-whisper
# Recommended: Use Large V3 Turbo model for best word-level timestamps

# Note: On Windows, you may also need:
# windows-curses  # Uncomment this line if you're on Windows